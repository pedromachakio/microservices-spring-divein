Criar imagem com dockerfile for some reason
docker build . -t nomeDaImagem

Criar imagem com pom dependencies set (	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<image>
						<name>${project.artifactId}</name>
					</image>
				</configuration>
			</plugin>
		</plugins>
	</build>)
mvn spring-boot:build-image

Correr imagem no porto X
docker run -p 8080(localport):8080(portodoDocker) nomedaImagem

Criar tag para poder dar push
docker tag nomeDaImagem:versãoDaImagem usernameDoDocker/nomeDoRepoDoDocker:nomeDaImagem

ex:
docker tag cards:latest machakio/bank:cards


Dar push
docker push username/nomeDoRepoDoDocker:nomeDaImagem
ex:
docker push machakio/bank:cards


Ver todas as imagens
docker images

Ver todos os containers
docker ps -a

Ver containers running
docker ps

Login no repo
docker login -u "username" -p "password" docker.io

